{
  "name": "autoscan",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "imports": {
    "#/*": "./src/*.js",
    "#tests/*": "./tests/*.js"
  },
  "scripts": {
    "check": "oxfmt -c oxfmt.config.ts --check && oxlint -c oxlint.config.ts --type-aware --type-check",
    "fmt": "oxfmt -c oxfmt.config.ts",
    "lint": "oxlint -c oxlint.config.ts --type-aware --type-check --fix --fix-suggestions",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "db:generate": "drizzle-kit generate",
    "knip": "knip",
    "test": "bun test ./tests",
    "test:coverage": "bun test ./tests --coverage --reporter=junit --reporter-outfile=./junit.xml",
    "test:watch": "bun test ./tests --watch",
    "postinstall": "bun2nix -o bun.nix"
  },
  "dependencies": {
    "drizzle-orm": "1.0.0-rc.4",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@commitlint/cli": "21.1.0",
    "@commitlint/config-conventional": "21.1.0",
    "@testcontainers/postgresql": "12.0.3",
    "@types/bun": "1.3.14",
    "bun2nix": "2.1.0",
    "drizzle-kit": "1.0.0-rc.4",
    "knip": "6.23.0",
    "lefthook": "2.1.9",
    "oxfmt": "0.56.0",
    "oxlint": "1.71.0",
    "oxlint-tsgolint": "0.23.0",
    "typescript": "6.0.3"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "packageManager": "bun@1.3.14",
  "trustedDependencies": [
    "lefthook"
  ]
}
